{
  "name": "ruleset",
  "version": "1.0.0",
  "private": true,
  "description": "A puzzle game where the interface is the puzzle.",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "play": "node server.mjs --open",
    "test": "node server.mjs --tests"
  },
  "engines": { "node": ">=18" }
}
